<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.menus7">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/lalbagh"
        android:label="@string/app_name"
        android:roundIcon="@drawable/lalbagh"
        android:supportsRtl="true"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/Theme.Menus7.Custom"
        tools:targetApi="31">

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDSrc32DR4GmP7_wKy-wiAPlqwFW4q1-UE" />

        <!-- ✅ Login activity should be the launcher activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.Menus7.NoActionBar"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- Google Sign-In Callback -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="com.example.menus7"
                    android:host="login-callback" />
            </intent-filter>
        </activity>

        <!-- ✅ SplashScreen activity -->
        <activity 
            android:name=".SplashScreenActivity" 
            android:exported="false"
            android:theme="@style/Theme.Menus7.NoActionBar"
            android:noHistory="true" />
            
        <!-- ✅ MainActivity -->
        <activity 
            android:name=".MainActivity" 
            android:exported="false"
            android:theme="@style/Theme.Menus7.NoActionBar"
            android:windowSoftInputMode="adjustResize" />
            
        <!-- ✅ SignupActivity -->
        <activity 
            android:name=".SignupActivity" 
            android:exported="false"
            android:theme="@style/Theme.Menus7.NoActionBar"
            android:windowSoftInputMode="adjustResize" />
            
        <!-- ✅ FAQActivity -->
        <activity 
            android:name=".FAQActivity" 
            android:exported="false"
            android:theme="@style/Theme.Menus7.NoActionBar"
            android:windowSoftInputMode="adjustResize" />
            
        <!-- ✅ EmergencyContactsActivity -->
        <activity 
            android:name=".EmergencyContactsActivity" 
            android:exported="false"
            android:theme="@style/Theme.Menus7.NoActionBar"
            android:windowSoftInputMode="adjustResize" />
            
        <!-- ✅ SafeZonesActivity -->
        <activity 
            android:name=".SafeZonesActivity" 
            android:exported="false"
            android:theme="@style/Theme.Menus7.NoActionBar"
            android:windowSoftInputMode="adjustResize" />
    </application>
</manifest>
